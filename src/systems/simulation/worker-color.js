"use strict";

module.exports = function(ecs, game) { // eslint-disable-line no-unused-vars
	ecs.addEach(function workerColor(entity, elapsed) { // eslint-disable-line no-unused-vars
	}, "fadePercent");
};
